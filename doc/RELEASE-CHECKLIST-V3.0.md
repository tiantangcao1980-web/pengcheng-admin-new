# MasterLife V3.0 发布前检查清单

**用途**：交付/发布前按本清单逐项确认，确保代码、文档与运行验证闭环。  
**关联**：任务状态见 `TASKS-V3.0.md`，📋 项对应「代码就绪，待环境验证」。

---

## 一、必选项（发布前必须完成）

### 1. 构建与测试

| 序号 | 检查项 | 命令/说明 | 状态 |
|------|--------|-----------|------|
| 1.1 | 后端编译（**须 JDK 17**） | `JAVA_HOME=<jdk17> mvn clean compile -DskipTests` 或一键脚本 `bash scripts/release-check.sh`；高版本 JDK 可能触发 Lombok TypeTag/Unsafe 错误，见 TASKS-V3.0 编译环境说明 | ⬜ |
| 1.2 | 后端单元测试 | `mvn test`（核心业务 + AI + 聊天 E2E 等） | ⬜ |
| 1.3 | 前端安装与构建 | `cd pengcheng-ui && npm ci && npm run build`（脚本已包含） | ⬜ |
| 1.4 | 前端 Lint/类型检查 | `npm run lint` / `npm run type-check`（若已配置；脚本会尝试执行 lint） | ⬜ |

### 2. 配置与安全

| 序号 | 检查项 | 说明 | 状态 |
|------|--------|------|------|
| 2.1 | 敏感信息未提交 | 智谱/数据库/Redis/OnlyOffice 等已改为环境变量，见 `.env.example` | ⬜ |
| 2.2 | 生产库账号 | 已按 `PRODUCTION-DB-USER.md` 创建专用账号、最小权限 | ⬜ |
| 2.3 | 安全审计 | 已阅读 `SECURITY-AUDIT.md`，无未处理的高危项 | ⬜ |

### 3. 功能与依赖

| 序号 | 检查项 | 说明 | 状态 |
|------|--------|------|------|
| 3.0 | 前端依赖（含 Tiptap） | `cd pengcheng-ui && npm ci` 或 `npm install`，确保 @tiptap/vue-3、@tiptap/starter-kit 已安装 | ⬜ |

### 4. 文档与流水线

| 序号 | 检查项 | 说明 | 状态 |
|------|--------|------|------|
| 4.1 | API 文档 | `API-REFERENCE.md` 与当前接口一致 | ⬜ |
| 4.2 | 部署文档 | `DEPLOYMENT.md` 可依步骤完成 Docker Compose / 手动部署 | ⬜ |
| 4.3 | 用户手册 | `USER-MANUAL.md` 覆盖主要功能 | ⬜ |
| 4.4 | CI/CD | GitHub Actions CI（构建+测试）、CD（镜像构建）可正常执行 | ⬜ |
| 4.5 | V24 看板列（可选） | 项目管理 → 项目详情 → 设置 → 看板列配置：新增/编辑/删除列，看板 Tab 按配置列展示 | ⬜ |
| 4.6 | **菜单与导航同步** | 若有菜单/归属变更：已按 `doc/MENU-CHANGE-PROCEDURE.md` 同步 **Flyway 迁移** + **sql/pengcheng-system.sql** + 文档（ARCHITECTURE/USER-MANUAL）；发布前导航栏与文档入口一致 | ⬜ |

---

## 二、环境验证项（代码就绪，需在目标环境中执行）

以下项在 TASKS-V3.0 中标记为 📋，完成对应环境准备后勾选。

### 5. AI 与 RAG

| 序号 | 检查项 | 前置条件 | 验证方式 | 状态 |
|------|--------|----------|----------|------|
| 5.1 | AI 功能（报表问答/文案/判客） | 配置 `ZHIPU_API_KEY` 或 DashScope 等 | 实际调用对应菜单/接口 | ⬜ |
| 5.2 | RAG 全流程 | 部署 PGVector + DashScope Embedding，配置正确 | 上传文档 → 切片 → 检索 → 生成回答 | ⬜ |
| 5.3 | 多轮对话上下文连续性 | Redis 正常、MemoryAdvisor 启用 | 多轮对话检查上下文是否连贯 | ⬜ |

### 6. 文件与协作

| 序号 | 检查项 | 前置条件 | 验证方式 | 状态 |
|------|--------|----------|----------|------|
| 6.1 | 文件预览（doc/docx/xls/xlsx/ppt/pptx/pdf） | 部署 kkFileView，nginx 反向代理正确 | 上传并预览各格式 | ⬜ |
| 6.2 | OnlyOffice 在线编辑（docx/xlsx/pptx） | 部署 OnlyOffice Document Server，JWT 配置一致 | 打开文档进行在线编辑并保存 | ⬜ |

### 7. 性能与压测（可选但建议执行）

| 序号 | 检查项 | 说明 | 状态 |
|------|--------|------|------|
| 7.1 | k6 冒烟压测 | `k6 run scripts/perf/api-smoke.js`，可选 `TOKEN`，见 `scripts/perf/README.md` | ⬜ |

---

## 三、可选/后续项

| 序号 | 检查项 | 说明 | 状态 |
|------|--------|------|------|
| 8.1 | AgentScope A2A 协议 | 待社区发布稳定 A2A SDK 后对接 | ⬜ 待社区 |
| 8.2 | 生产数据备份与恢复演练 | 按 DEPLOYMENT 备份策略做一次恢复演练 | ⬜ |

---

## 四、收口结论

- **可发布条件**：一、二中的必选项（1.x、2.x、3.x、4.x）全部通过，且 5.1（AI 功能）已在目标环境验证。
- **建议**：5.2（RAG）、5.3（多轮）、6.1/6.2（预览/OnlyOffice）、7.1（压测）在具备环境时补齐验证并勾选。
- **签署**：发布负责人填写日期与结论。

| 项目 | 内容 |
|------|------|
| 检查完成日期 | ____年__月__日 |
| 发布结论 | ☐ 通过发布 / ☐ 有条件发布（说明：__________） / ☐ 不通过 |
